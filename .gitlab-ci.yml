variables:
  HOST: "shoya85.starfree.jp"
  USERNAME: "shoya85.starfree.jp"
  PASSWORD: "anua10fweoi32"

deploy:
  script:
    - apt-get update -qq && apt-get install -y -qq lftp
    - lftp -c "set ftp:ssl-allow no; open -u $USERNAME,$PASSWORD $HOST; mput sys/*; mput pub/*"
  only:
    - master
